# Backend
PORT=3000

# PostgreSQL (docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=prueba_fs
DB_USER=postgres
DB_PASSWORD=postgres

POKEAPI_BASE_URL=https://pokeapi.co/api/v2

# CORS (origen del frontend). Puedes listar varios separados por coma
FRONTEND_ORIGIN=http://localhost:5173